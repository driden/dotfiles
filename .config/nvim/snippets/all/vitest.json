{
  "describe(vitest)": {
    "prefix": "dsc",
    "body": [
      "import { describe, expect, it, vi } from \"vitest\";",
      "",
      "describe(\"${0:suite}\", () => {",
      "\tit(\"${1:test}\", async () => {",
      "$2",
      "\t});",
      "});"
    ],
    "description": "Create a new test suite"
  },
  "it(vitest)": {
    "prefix": ">it",
    "body": [
      "it(\"${0:test}\", async () => {",
      "$2",
      "});"
    ],
    "description": "Create a new test suite"
  }
}
